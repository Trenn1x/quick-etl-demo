services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: etl_pass
      POSTGRES_DB: etl_demo
    ports:
      - "5432:5432"
  adminer:
    image: adminer
    ports:
      - "8080:8080"

